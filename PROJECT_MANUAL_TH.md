# 🏥 แพลตฟอร์มการเรียนรู้ทางการแพทย์โรงพยาบาลจังหวัรชม่อง (med-km)
## คู่มือโครงการและคู่มือผู้ใช้ที่ครอบคลุม

**เวอร์ชัน:** 1.0  
**อัปเดตล่าสุด:** 18 กุมภาพันธ์ 2569  
**รหัสโครงการ:** med-km

---

## สารบัญ

1. [ภาพรวมโครงการ](#ภาพรวมโครงการ)
2. [สถาปัตยกรรมระบบ](#สถาปัตยกรรมระบบ)
3. [การติดตั้งและการตั้งค่า](#การติดตั้งและการตั้งค่า)
4. [การทำให้ใช้งานได้ในสภาพแวดล้อมการผลิต](#การทำให้ใช้งานได้ในสภาพแวดล้อมการผลิต)
5. [บทบาทและความรับผิดชอบของผู้ใช้](#บทบาทและความรับผิดชอบของผู้ใช้)
6. [คู่มือผู้ใช้ตามบทบาท](#คู่มือผู้ใช้ตามบทบาท)
7. [เอกสาร API](#เอกสาร-api)
8. [การแก้ปัญหา](#การแก้ปัญหา)
9. [แนวทางความปลอดภัย](#แนวทางความปลอดภัย)
10. [การบำรุงรักษาและการสนับสนุน](#การบำรุงรักษาและการสนับสนุน)

---

## ภาพรวมโครงการ

### 🎯 วัตถุประสงค์

**แพลตฟอร์มการเรียนรู้ทางการแพทย์โรงพยาบาลจังหวัรชม่อง (med-km)** เป็นระบบการศึกษาดิจิทัลสมัยใหม่และครอบคลุมที่ออกแบบมาเพื่ออำนวยความสะดวกในการศึกษาทางการแพทย์อย่างต่อเนื่องและการพัฒนาวิชาชีพสำหรับมืออาชีพด้านสุขภาพและนักศึกษาแพทย์ แพลตฟอร์มนี้สะพานความรู้ความเชี่ยวชาญทางการแพทย์แบบดั้งเดิมกับเทคโนโลยีดิจิทัลที่ล้ำสมัยเพื่อสร้างสภาพแวดล้อมการเรียนรู้ที่น่าสนใจและโต้ตอบได้

### 📋 วัตถุประสงค์หลัก

- **ความเป็นเลิศทางการแพทย์**: ให้บริการข้อสอบทางการแพทย์ที่ครอบคลุมและมีคุณภาพสูงซึ่งจัดเก็บโดยมืออาชีพด้านสุขภาพ
- **การเรียนรู้อย่างชาญฉลาด**: ส่งมอบการวิเคราะห์ที่ขับเคลื่อนด้วย AI เพื่อติดตามความก้าวหน้าและระบุช่องว่างการเรียนรู้
- **เครือข่ายสัมพันธ์ร่วมมือ**: สนับสนุนการแลกเปลี่ยนความรู้ระหว่างจิตแพทย์ผู้ตรวจสอบ และนักเรียน
- **ข้อมูลเสริมทั้งแบบเรียลไทม์**: ให้การประเมินข้อสอบทันทีพร้อมคำอธิบายโดยละเอียด
- **ข้อมูลเชิงลึกที่ขับเคลื่อนด้วยข้อมูล**: สร้างภาพเชิงความเคลื่อนไหวของแนวโน้มการเรียนรู้และเมตริกประสิทธิภาพ
- **ปลอดภัยและเชื่อถือได้**: ปกป้องข้อมูลการแพทย์ด้วยความปลอดภัยระดับองค์กร

### ✨ คุณลักษณะหลัก

| คุณลักษณะ | คำอธิบาย |
|---------|---------|
| **การจัดการข้อสอบ** | สร้าง ทำข้อสอบ และวิเคราะห์ข้อสอบทางการแพทย์ |
| **แบงค์คำถาม** | ที่เก็บเก็บข้อมูลที่ครอบคลุมของคำถามทางการแพทย์ที่มีรูปภาพ |
| **วิश्लेषण นักเรียน** | ติดตามความก้าวหน้า แนวโน้ม และเมตริกประสิทธิภาพ |
| **แดชบอร์ดจิตแพทย์** | สร้างคำถามและตรวจสอบประสิทธิภาพของนักเรียน |
| **คิวโอเอ** | ตรวจสอบและอนุมัติคำถามเพื่อการปรับปรุงคุณภาพ |
| **แพนลประสาน** | การจัดการผู้ใช้และการดำเนินงานระบบ |
| **รายงาน PDF** | สร้างรายงานข้อสอบและใบรับรองที่พิมพ์ได้ |
| **การนำทางแบบแอคทีฟ** | การเน้นภาพของหน้าปัจจุบันในเมนูนำทาง |

---

## สถาปัตยกรรมระบบ

### 🏗️ สแต็กเทคโนโลยี

**ส่วนหน้า (Frontend):**
- เฟรมเวิร์ก: React 18 พร้อม Vite
- การเข้าชม: React Router DOM
- ไคลเอนต์ HTTP: Axios
- รูปแบบ: CSS แบบกำหนดเองพร้อมธีมสีเขียว (#15803d)
- การออกแบบที่ตอบสนอง: สมดุลมือถือ แท็บเล็ต และเดสก์ท็อป

**แบ็กเอนด (Backend):**
- รันไทม์: Node.js
- เฟรมเวิร์ก: Express.js
- ฐานข้อมูล: การจัดเก็บไฟล์ JSON (พัฒนา)
- การสร้าง PDF: PDFKit
- การตรวจสอบสิทธิ์: Bearer Token (JWT-compatible)
- พอร์ต: 3001 (ค่าเริ่มต้น)

**โครงสร้างพื้นฐาน (Infrastructure):**
- พอร์ตส่วนหน้า: 5173 (พัฒนา)
- พอร์ตแบ็กเอนด: 3001 (พัฒนา)
- การสนับสนุน Docker: ไฟล์ Dockerfile และ docker-compose.yml พร้อมใช้งาน
- การควบคุมเวอร์ชัน: Git

### 📁 โครงสร้างโครงการ

```
med-km/
├── frontend/                          # แอปพลิเคชัน React
│   ├── src/
│   │   ├── App.jsx                   # เราเตอร์แอปหลักและการนำทาง
│   │   ├── api.js                    # ไคลเอนต์ Axios API
│   │   ├── main.jsx                  # จุดเข้า React
│   │   ├── pages/                    # ส่วนประกอบหน้า
│   │   │   ├── HomePage.jsx          # แดชบอร์ดต้อนรับ
│   │   │   ├── Login.jsx             # การตรวจสอบสิทธิ์
│   │   │   ├── ExamsList.jsx         # เรียกดูข้อสอบ
│   │   │   ├── ExamTake.jsx          # ทำข้อสอบ
│   │   │   ├── ExamBuilder.jsx       # สร้างข้อสอบ (สตาฟเท่านั้น)
│   │   │   ├── UploadQuestion.jsx    # อัปโหลดคำถาม (จิตแพทย์)
│   │   │   ├── ManageQuestionsExams.jsx  # จัดการเนื้อหา
│   │   │   ├── ModeratorQueue.jsx    # คิวตรวจสอบ (ผู้ดูแล)
│   │   │   ├── StudentStats.jsx      # วิเคราะห์นักเรียน
│   │   │   ├── ClinicianDashboard.jsx# วิเคราะห์จิตแพทย์
│   │   │   └── AdminUserManagement.jsx # การจัดการผู้ใช้
│   │   └── styles.css                # รูปแบบทั่วไป
│   ├── public/                       # สินทรัพย์คงที่
│   │   └── logo.png                  # โลโก้โรงพยาบาลจังหวัรชม่อง
│   ├── package.json
│   └── vite.config.js
│
├── backend/
│   ├── mock-api/                     # แบ็กเอนด Express
│   │   ├── index.js                  # ไฟล์เซิร์ฟเวอร์หลัก
│   │   ├── package.json
│   │   └── data/                     # ไฟล์ข้อมูล JSON
│   │       ├── users.json
│   │       ├── questions.json
│   │       ├── exams.json
│   │       ├── student_exams.json
│   │       ├── specialties.json
│   │       ├── audit_logs.json
│   │       └── question_images/
│   │
│   └── laravel-real/                 # (ตัวเลือก) แบ็กเอนดการผลิต
│       ├── app/Models/               # โมเดลฐานข้อมูล
│       ├── app/Http/Controllers/     # ตัวควบคุม API
│       ├── database/migrations/      # สคีมาฐานข้อมูล
│       ├── routes/api.php            # เส้นทาง API
│       └── composer.json
│
├── README.md                         # ภาพรวมโครงการ
└── PROJECT_MANUAL_TH.md              # ไฟล์นี้

```

### 🔐 การไหลของการตรวจสอบสิทธิ์

```
1. ผู้ใช้ป้อนรหัส Thai ID / อีเมล + รหัสผ่าน
2. แบ็กเอนดตรวจสอบข้อมูลประจำตัว
3. หากสำเร็จ: สร้าง Bearer Token (JWT)
4. โทเค็นเก็บไว้ใน localStorage (ส่วนหน้า)
5. คำขอ API ทั้งหมดรวมถึง Authorization: Bearer {token}
6. โทเค็นหมดอายุหลังจากเซッชันสิ้นสุดหรือออกจากระบบด้วยตนเอง
```

---

## การติดตั้งและการตั้งค่า

### ✅ ข้อกำหนดเบื้องต้น

- **Node.js**: v14.0 หรือสูงกว่า ([ดาวน์โหลด](https://nodejs.org/))
- **npm**: v6.0 หรือสูงกว่า (รวมอยู่ใน Node.js)
- **Git**: สำหรับการควบคุมเวอร์ชัน ([ดาวน์โหลด](https://git-scm.com/))
- **Terminal/CMD**: PowerShell, CMD หรือ Bash
- **พื้นที่ดิสก์**: ขั้นต่ำ 500MB (ไม่รวม node_modules)

### 📥 ขั้นตอนการติดตั้ง

#### ขั้นตอนที่ 1: โคลนที่เก็บข้อมูล
```bash
git clone <repository-url>
cd vscode_project
```

#### ขั้นตอนที่ 2: ติดตั้งความต้องการของแบ็กเอนด
```bash
cd backend/mock-api
npm install
```

#### ขั้นตอนที่ 3: ติดตั้งความต้องการของส่วนหน้า
```bash
cd ../../frontend
npm install
```

#### ขั้นตอนที่ 4: ตรวจสอบการติดตั้ง
ตรวจสอบแบ็กเอนด:
```bash
cd ../backend/mock-api
node --check index.js
```

สร้างส่วนหน้า:
```bash
cd ../../frontend
npm run build
```

### 🚀 การเรียกใช้สภาพแวดล้อมการพัฒนา

#### ตัวเลือกที่ 1: เรียกใช้ทั้งส่วนหน้าและแบ็กเอนด

**Terminal ที่ 1 - แบ็กเอนด:**
```bash
cd backend/mock-api
npm run start
```
แบ็กเอนดเรียกใช้บน: `http://localhost:3001`

**Terminal ที่ 2 - ส่วนหน้า:**
```bash
cd frontend
npm run dev
```
ส่วนหน้าเรียกใช้บน: `http://localhost:5173`

#### ตัวเลือกที่ 2: เรียกใช้เฉพาะส่วนหน้า (แบ็กเอนดมีการทำให้ใช้งานได้แล้ว)
```bash
cd frontend
npm run dev
```
อัปเดต `frontend/src/api.js` เพื่อชี้ไปยัง URL แบ็กเอนดการผลิต

### 🔧 การตั้งค่า

#### การตั้งค่าแบ็กเอนด (`backend/mock-api/index.js`)
```javascript
// การตั้งค่า CORS
app.use(cors({
  origin: '*',
  credentials: true
}));

// พอร์ตเซิร์ฟเวอร์
const PORT = process.env.PORT || 3001;

// การตรวจสอบสิทธิ์
const authMiddleware = (req, res, next) => { ... };
```

#### การตั้งค่า API ส่วนหน้า (`frontend/src/api.js`)
```javascript
import axios from 'axios'

const api = axios.create({
  baseURL: 'http://localhost:3001/api',
  timeout: 10000
})

// เพิ่มโทเค็นลงในคำขอ
api.interceptors.request.use(config => {
  const token = localStorage.getItem('token')
  if (token) {
    config.headers.Authorization = `Bearer ${token}`
  }
  return config
})

export default api
```

---

## การทำให้ใช้งานได้ในสภาพแวดล้อมการผลิต

### 🐳 การทำให้ใช้งานได้ของ Docker (แนะนำ)

#### ข้อกำหนดเบื้องต้น
- ติดตั้ง Docker ([ดาวน์โหลด](https://www.docker.com/products/docker-desktop))
- ติดตั้ง Docker Compose (รวมอยู่ใน Docker Desktop)

#### ขั้นตอนที่ 1: สร้างอิมเมจ Docker
```bash
cd backend/laravel-real
docker-compose build
```

#### ขั้นตอนที่ 2: เริ่มบริการ
```bash
docker-compose up -d
```

#### ขั้นตอนที่ 3: เรียกใช้การย้ายข้อมูล (ครั้งแรก)
```bash
docker-compose exec app php artisan migrate
docker-compose exec app php artisan seed
```

#### ขั้นตอนที่ 4: เข้าถึงแอปพลิเคชัน
- ส่วนหน้า: `http://localhost`
- API แบ็กเอนด: `http://localhost/api`

### 📦 การทำให้ใช้งานได้ของเซิร์ฟเวอร์แบบดั้งเดิม

#### การทำให้ใช้งานได้ของส่วนหน้า (Nginx/Apache)

**ขั้นตอนที่ 1: สร้างสำหรับการผลิต**
```bash
cd frontend
npm run build
```
สิ่งนี้สร้างไฟล์ที่เหมาะสมในโฟลเดอร์ `dist/`

**ขั้นตอนที่ 2: ทำให้ใช้งานได้ในเซิร์ฟเวอร์เว็บ**

**การตั้งค่า Nginx:**
```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        root /var/www/med-km/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    location /api {
        proxy_pass http://backend-server:3001;
        proxy_set_header Authorization $http_authorization;
    }
}
```

**การตั้งค่า Apache:**
```apache
<VirtualHost *:80>
    ServerName your-domain.com
    DocumentRoot /var/www/med-km/frontend/dist
    
    <Directory /var/www/med-km/frontend/dist>
        RewriteEngine On
        RewriteBase /
        RewriteRule ^index\.html$ - [L]
        RewriteCond %{REQUEST_FILENAME} !-f
        RewriteCond %{REQUEST_FILENAME} !-d
        RewriteRule . /index.html [L]
    </Directory>

    ProxyPreserveHost On
    ProxyPass /api http://backend-server:3001/api
    ProxyPassReverse /api http://backend-server:3001/api
</VirtualHost>
```

#### การทำให้ใช้งานได้ของแบ็กเอนด (Node.js)

**ตัวเลือก A: การใช้ PM2 (แนะนำ)**
```bash
npm install -g pm2

cd backend/mock-api
npm install --production
pm2 start index.js --name "med-km-api"
pm2 save
pm2 startup
```

**ตัวเลือก B: การใช้ systemd (Linux)**
สร้าง `/etc/systemd/system/med-km-api.service`:
```ini
[Unit]
Description=Chomthong Medical Learning Platform API
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/med-km/backend/mock-api
ExecStart=/usr/bin/node index.js
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

จากนั้น:
```bash
sudo systemctl daemon-reload
sudo systemctl enable med-km-api
sudo systemctl start med-km-api
```

#### ตัวแปรสภาพแวดล้อม (.env)
```bash
# ส่วนหน้า (.env)
VITE_API_URL=https://your-domain.com/api
VITE_APP_NAME=med-km

# แบ็กเอนด (.env)
NODE_ENV=production
PORT=3001
CORS_ORIGIN=https://your-domain.com
JWT_SECRET=your-secure-secret-key-here
```

### 🔒 การตั้งค่า SSL/HTTPS

**การใช้ Let's Encrypt (ฟรี):**
```bash
sudo apt-get install certbot python3-certbot-nginx
sudo certbot certonly --nginx -d your-domain.com
```

**การตั้งค่า SSL ของ Nginx:**
```nginx
server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;
    
    # ... คอนฟิกที่เหลือ
}

# เปลี่ยนเส้นทาง HTTP เป็น HTTPS
server {
    listen 80;
    server_name your-domain.com;
    return 301 https://$server_name$request_uri;
}
```

### 📊 การตั้งค่าฐานข้อมูล (การผลิต)

สำหรับการผลิต ให้ย้ายจาก JSON ไปยังฐานข้อมูลที่เหมาะสม:

**การอพเกรด PostgreSQL:**
```bash
cd backend/laravel-real
php artisan config:cache
php artisan migrate --force
php artisan db:seed
```

**การอพเกรด MySQL:**
```bash
# อัปเดต .env ด้วยข้อมูลประจำตัว MySQL
DB_CONNECTION=mysql
DB_HOST=mysql-server
DB_DATABASE=med_km
DB_USERNAME=med_km_user
DB_PASSWORD=secure-password

# เรียกใช้การย้ายข้อมูล
php artisan migrate --force
```

---

## 🏗️ ตัวเลือกสถาปัตยกรรมแบ็กเอนด์และการติดตั้งในเซิร์ฟเวอร์ Linux สำหรับการผลิต

### ทำความเข้าใจสถาปัตยกรรมแบ็กเอนด์สองแบบ

โครงการนี้มี **แบ็กเอนด์สองรูปแบบ** ที่ให้บริการ REST API endpoints เหมือนกัน:

#### 1. **Mock API (Node.js + Express)** 
- **วัตถุประสงค์:** การพัฒนา การสร้างต้นแบบ และการทดสอบ
- **เทคโนโลยี:** Node.js, Express.js
- **การจัดเก็บ:** ไฟล์ JSON (ไม่จำเป็นต้องมีฐานข้อมูล)
- **พอร์ต:** 3001 (ค่าเริ่มต้น)
- **กรณีการใช้งาน:** การพัฒนาอย่างรวดเร็ว การทดสอบส่วนหน้า การสาธิต

#### 2. **Laravel Backend (PHP)**
- **วัตถุประสงค์:** การติดตั้งใช้งานจริง
- **เทคโนโลยี:** PHP 8.1+, Laravel 10, MySQL/PostgreSQL
- **การจัดเก็บ:** ฐานข้อมูลเชิงสัมพันธ์พร้อม ORM
- **พอร์ต:** 8000 (ค่าเริ่มต้น)
- **กรณีการใช้งาน:** สภาพแวดล้อมการผลิตระดับองค์กร

### ตารางเปรียบเทียบแบ็กเอนด์

| คุณสมบัติ | Mock API (Node.js) | Laravel (PHP) |
|---------|-------------------|---------------|
| **เวลาตั้งค่า** | ⚡ 2-5 นาที | 🕐 15-30 นาที |
| **ต้องการฐานข้อมูล** | ❌ ไม่ (ไฟล์ JSON) | ✅ ใช่ (MySQL/PostgreSQL) |
| **พร้อมสำหรับการผลิต** | ❌ ไม่แนะนำ | ✅ แนะนำ |
| **ความเร็วในการพัฒนา** | ✅ รวดเร็วมาก | ⚠️ ปานกลาง |
| **ความสามารถในการขยาย** | ❌ จำกัด (อินสแตนซ์เดียว) | ✅ สูง (ขยายแนวนอน) |
| **ประสิทธิภาพ** | ⚠️ ดีสำหรับ < 100 ผู้ใช้ | ✅ ยอดเยี่ยมสำหรับ 1000+ ผู้ใช้ |
| **คุณสมบัติ** | 📦 CRUD พื้นฐาน | 🎁 เฟรมเวิร์กเต็มรูปแบบ (queues, cache, jobs) |
| **ความคงอยู่ของข้อมูล** | ⚠️ ไฟล์-based | ✅ ฐานข้อมูลรองรับ ACID |
| **ผู้ใช้งานพร้อมกัน** | ⚠️ < 50 | ✅ 500+ |
| **ความปลอดภัย** | ⚠️ การตรวจสอบ token พื้นฐาน | ✅ Laravel Sanctum, การป้องกัน CSRF |
| **การสำรองข้อมูลและการกู้คืน** | ⚠️ คัดลอกไฟล์ด้วยตนเอง | ✅ ถ่าย dump ฐานข้อมูลและทำซ้ำ |
| **API Endpoints** | ✅ เหมือนกับ Laravel | ✅ เหมือนกับ Mock API |
| **การตรวจสอบ** | ⚠️ การบันทึกพื้นฐาน | ✅ Laravel Telescope, logs |
| **การบำรุงรักษา** | ✅ น้อยที่สุด | ⚠️ ต้องการอัปเดตเป็นประจำ |

### เมื่อใดควรใช้แบ็กเอนด์แต่ละแบบ

**ใช้ Mock API (Node.js) เมื่อ:**
- 🚀 การสร้างต้นแบบและการพัฒนาอย่างรวดเร็ว
- 🧪 การทดสอบส่วนหน้าโดยไม่ต้องตั้งค่าฐานข้อมูล
- 📊 การนำเสนอสาธิตและ proof-of-concept
- 👨‍💻 สภาพแวดล้อมการพัฒนาในเครื่อง
- 📝 ทีมเล็ก (< 10 ผู้ใช้)

**ใช้ Laravel (PHP) เมื่อ:**
- 🏥 การติดตั้งใช้งานจริงสำหรับโรงพยาบาล/คลินิก
- 👥 สภาพแวดล้อมผู้ใช้หลายคน (50+ ผู้ใช้)
- 🔒 ความต้องการด้านความปลอดภัยและการปฏิบัติตามข้อกำหนด (HIPAA, การป้องกัน PHI)
- 📈 ความสามารถในการขยายและประสิทธิภาพมีความสำคัญ
- 💾 ความสมบูรณ์ของข้อมูลและการสำรองข้อมูลมีความจำเป็น
- 🔄 การรวมเข้ากับโครงสร้างพื้นฐาน PHP ที่มีอยู่

---

## 🐧 คู่มือการติดตั้งใช้งานจริงใน Linux แบบสมบูรณ์

คู่มือนี้ครอบคลุมการติดตั้งแพลตฟอร์มการเรียนรู้ทางการแพทย์บนเซิร์ฟเวอร์ Ubuntu/Debian Linux

### ข้อกำหนดเบื้องต้น

- เซิร์ฟเวอร์ Ubuntu 20.04+ หรือ Debian 11+
- การเข้าถึง Root หรือ sudo
- โดเมนที่กำหนดค่าแล้ว (ไม่บังคับแต่แนะนำ)
- ขั้นต่ำ RAM 2GB, CPU 2 cores, พื้นที่ดิสก์ 20GB

---

### ส่วนที่ 1: การตั้งค่าเซิร์ฟเวอร์เบื้องต้น

#### ขั้นตอนที่ 1: อัปเดตระบบ
```bash
sudo apt update && sudo apt upgrade -y
sudo apt install -y curl wget git vim ufw
```

#### ขั้นตอนที่ 2: สร้างผู้ใช้แอปพลิเคชัน
```bash
sudo adduser medkm
sudo usermod -aG sudo medkm
sudo su - medkm
```

#### ขั้นตอนที่ 3: กำหนดค่าไฟร์วอลล์
```bash
sudo ufw allow OpenSSH
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable
sudo ufw status
```

#### ขั้นตอนที่ 4: ติดตั้ง Node.js (จำเป็นสำหรับทั้งสองแบ็กเอนด์)
```bash
# ติดตั้ง Node.js 18.x LTS
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# ตรวจสอบการติดตั้ง
node --version  # ควรแสดง v18.x.x
npm --version   # ควรแสดง 9.x.x
```

#### ขั้นตอนที่ 5: ติดตั้งเว็บเซิร์ฟเวอร์ Nginx
```bash
sudo apt install -y nginx
sudo systemctl start nginx
sudo systemctl enable nginx
sudo systemctl status nginx
```

#### ขั้นตอนที่ 6: โคลน Repository
```bash
cd /var/www
sudo mkdir -p medkm
sudo chown medkm:medkm medkm
cd medkm
git clone https://github.com/your-org/med-km.git .
```

---

### ส่วนที่ 2A: ติดตั้งด้วย Mock API (แบ็กเอนด์ Node.js)

**ใช้ตัวเลือกนี้สำหรับการพัฒนา/ทดสอบหรือการติดตั้งขนาดเล็ก**

#### ขั้นตอนที่ 1: ติดตั้ง Dependencies ของแบ็กเอนด์
```bash
cd /var/www/medkm/backend/mock-api
npm install --production
```

#### ขั้นตอนที่ 2: สร้างไฟล์ Environment ของแบ็กเอนด์
```bash
nano .env
```
เพิ่ม:
```bash
NODE_ENV=production
PORT=3001
CORS_ORIGIN=https://your-domain.com
```

#### ขั้นตอนที่ 3: ทดสอบแบ็กเอนด์
```bash
node index.js
# ควรเห็น: Server listening on port 3001
# กด Ctrl+C เพื่อหยุด
```

#### ขั้นตอนที่ 4: ติดตั้ง PM2 Process Manager
```bash
sudo npm install -g pm2
```

#### ขั้นตอนที่ 5: เริ่มแบ็กเอนด์ด้วย PM2
```bash
cd /var/www/medkm/backend/mock-api
pm2 start index.js --name medkm-api
pm2 save
pm2 startup
# คัดลอกและรันคำสั่งที่ PM2 แสดง
```

#### ขั้นตอนที่ 6: ตรวจสอบสถานะแบ็กเอนด์
```bash
pm2 status
pm2 logs medkm-api
```

#### ขั้นตอนที่ 7: สร้างส่วนหน้า
```bash
cd /var/www/medkm/frontend

# สร้างไฟล์ environment สำหรับการผลิต
nano .env
```
เพิ่ม:
```bash
VITE_API_BASE_URL=https://your-domain.com/api
```

สร้าง:
```bash
npm install
npm run build
# สร้างโฟลเดอร์ dist/ พร้อมไฟล์ที่เหมาะสม
```

#### ขั้นตอนที่ 8: กำหนดค่า Nginx สำหรับแบ็กเอนด์ Node.js
```bash
sudo nano /etc/nginx/sites-available/medkm
```
เพิ่ม:
```nginx
server {
    listen 80;
    server_name your-domain.com;
    
    # ส่วนหน้า (React build)
    location / {
        root /var/www/medkm/frontend/dist;
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "public, max-age=3600";
    }
    
    # แบ็กเอนด์ API (Node.js)
    location /api {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_read_timeout 300s;
        proxy_connect_timeout 75s;
    }
    
    # Health check endpoint
    location /health {
        access_log off;
        return 200 "healthy\n";
        add_header Content-Type text/plain;
    }
}
```

เปิดใช้งานไซต์:
```bash
sudo ln -s /etc/nginx/sites-available/medkm /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### ขั้นตอนที่ 9: ติดตั้งใบรับรอง SSL (Let's Encrypt)
```bash
sudo apt install -y certbot python3-certbot-nginx
sudo certbot --nginx -d your-domain.com
# ทำตามคำแนะนำ เลือกเปลี่ยนเส้นทาง HTTP ไปยัง HTTPS
```

#### ขั้นตอนที่ 10: ตรวจสอบการติดตั้ง
```bash
# ตรวจสอบแบ็กเอนด์
curl http://localhost:3001/api/health

# ตรวจสอบทั้งระบบ
curl https://your-domain.com
curl https://your-domain.com/api/health
```

---

### ส่วนที่ 2B: ติดตั้งด้วยแบ็กเอนด์ Laravel (แนะนำสำหรับการผลิต)

**ใช้ตัวเลือกนี้สำหรับการติดตั้งใช้งานจริงพร้อมฐานข้อมูล**

#### ขั้นตอนที่ 1: ติดตั้ง PHP และ Dependencies
```bash
sudo apt install -y php8.1 php8.1-fpm php8.1-cli php8.1-common \
    php8.1-mysql php8.1-xml php8.1-curl php8.1-gd php8.1-mbstring \
    php8.1-zip php8.1-bcmath php8.1-intl php8.1-sqlite3

# ตรวจสอบเวอร์ชัน PHP
php -v  # ควรแสดง PHP 8.1.x
```

#### ขั้นตอนที่ 2: ติดตั้ง Composer
```bash
curl -sS https://getcomposer.org/installer | php
sudo mv composer.phar /usr/local/bin/composer
composer --version
```

#### ขั้นตอนที่ 3: ติดตั้งฐานข้อมูล (MySQL)
```bash
sudo apt install -y mysql-server
sudo mysql_secure_installation
# ทำตามคำแนะนำ: ตั้งรหัสผ่าน root, ลบผู้ใช้นิรนาม ฯลฯ
```

สร้างฐานข้อมูล:
```bash
sudo mysql
```
ใน MySQL:
```sql
CREATE DATABASE medkm CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'medkm_user'@'localhost' IDENTIFIED BY 'your_secure_password';
GRANT ALL PRIVILEGES ON medkm.* TO 'medkm_user'@'localhost';
FLUSH PRIVILEGES;
EXIT;
```

#### ขั้นตอนที่ 4: ติดตั้ง Laravel Dependencies
```bash
cd /var/www/medkm/backend/laravel-real
composer install --optimize-autoloader --no-dev
```

#### ขั้นตอนที่ 5: กำหนดค่า Laravel Environment
```bash
cp .env.example .env
nano .env
```
กำหนดค่า:
```bash
APP_NAME="Medical Learning Platform"
APP_ENV=production
APP_DEBUG=false
APP_URL=https://your-domain.com

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=medkm
DB_USERNAME=medkm_user
DB_PASSWORD=your_secure_password

CACHE_DRIVER=file
QUEUE_CONNECTION=database
SESSION_DRIVER=file
SESSION_LIFETIME=120

# สร้างในขั้นตอนถัดไป
APP_KEY=
```

#### ขั้นตอนที่ 6: สร้าง Application Key
```bash
php artisan key:generate
php artisan config:cache
```

#### ขั้นตอนที่ 7: รันการย้ายฐานข้อมูล
```bash
php artisan migrate --force
php artisan db:seed --force
```

#### ขั้นตอนที่ 8: ตั้งค่าสิทธิ์
```bash
sudo chown -R www-data:www-data /var/www/medkm/backend/laravel-real/storage
sudo chown -R www-data:www-data /var/www/medkm/backend/laravel-real/bootstrap/cache
sudo chmod -R 775 /var/www/medkm/backend/laravel-real/storage
sudo chmod -R 775 /var/www/medkm/backend/laravel-real/bootstrap/cache
```

#### ขั้นตอนที่ 9: กำหนดค่า Nginx สำหรับ Laravel
```bash
sudo nano /etc/nginx/sites-available/medkm
```
เพิ่ม:
```nginx
server {
    listen 80;
    server_name your-domain.com;
    root /var/www/medkm/frontend/dist;
    index index.html;
    
    # ส่วนหน้า
    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "public, max-age=3600";
    }
    
    # แบ็กเอนด์ API (Laravel)
    location /api {
        alias /var/www/medkm/backend/laravel-real/public;
        try_files $uri $uri/ @laravel;
        
        location ~ \.php$ {
            include snippets/fastcgi-php.conf;
            fastcgi_pass unix:/var/run/php/php8.1-fpm.sock;
            fastcgi_param SCRIPT_FILENAME /var/www/medkm/backend/laravel-real/public/index.php;
            include fastcgi_params;
        }
    }
    
    location @laravel {
        rewrite /api/(.*)$ /api/index.php?/$1 last;
    }
    
    # ปฏิเสธการเข้าถึงไฟล์ที่ละเอียดอ่อน
    location ~ /\.(?!well-known).* {
        deny all;
    }
}
```

เปิดใช้งานและโหลดใหม่:
```bash
sudo ln -s /etc/nginx/sites-available/medkm /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

#### ขั้นตอนที่ 10: สร้างส่วนหน้าสำหรับ Laravel
```bash
cd /var/www/medkm/frontend
nano .env
```
อัปเดต API URL:
```bash
VITE_API_BASE_URL=https://your-domain.com/api
```

สร้าง:
```bash
npm install
npm run build
```

#### ขั้นตอนที่ 11: ติดตั้ง SSL
```bash
sudo certbot --nginx -d your-domain.com
```

#### ขั้นตอนที่ 12: ตั้งค่า Queue Worker (ไม่บังคับแต่แนะนำ)
```bash
sudo nano /etc/systemd/system/medkm-queue.service
```
เพิ่ม:
```ini
[Unit]
Description=Medical Learning Platform Queue Worker
After=network.target mysql.service

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/medkm/backend/laravel-real
ExecStart=/usr/bin/php artisan queue:work --sleep=3 --tries=3 --max-time=3600
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

เปิดใช้งานและเริ่ม:
```bash
sudo systemctl daemon-reload
sudo systemctl enable medkm-queue
sudo systemctl start medkm-queue
sudo systemctl status medkm-queue
```

#### ขั้นตอนที่ 13: ตั้งค่า Scheduler (Cron Job)
```bash
sudo crontab -e -u www-data
```
เพิ่ม:
```cron
* * * * * cd /var/www/medkm/backend/laravel-real && php artisan schedule:run >> /dev/null 2>&1
```

---

### ส่วนที่ 3: การสลับระหว่างแบ็กเอนด์

#### จาก Mock API ไป Laravel

1. **เตรียมการย้ายข้อมูล** (หากจำเป็น):
```bash
# ส่งออกข้อมูลจากไฟล์ JSON ของ Mock API
cd /var/www/medkm/backend/mock-api/data
cp users.json users_backup.json
cp questions.json questions_backup.json
cp exams.json exams_backup.json
```

2. **อัปเดตการกำหนดค่าส่วนหน้า**:
```bash
cd /var/www/medkm/frontend
nano .env
```
เปลี่ยน:
```bash
# จาก:
VITE_API_BASE_URL=https://your-domain.com/api

# เป็น (ถ้า Laravel อยู่ที่พอร์ตอื่น):
VITE_API_BASE_URL=https://your-domain.com/api
```

3. **สร้างส่วนหน้าใหม่**:
```bash
npm run build
```

4. **หยุด Mock API**:
```bash
pm2 stop medkm-api
pm2 delete medkm-api
```

5. **อัปเดตการกำหนดค่า Nginx** (เปลี่ยน proxy_pass จาก 3001 ไป Laravel)

6. **โหลด Nginx ใหม่**:
```bash
sudo systemctl reload nginx
```

7. **ย้ายข้อมูลไปยังฐานข้อมูล Laravel** (ด้วยตนเองหรือสคริปต์):
```bash
# เขียนสคริปต์การย้ายข้อมูลที่กำหนดเองหรือใส่ข้อมูลด้วยตนเอง
php artisan tinker
# ใช้ Eloquent เพื่อใส่ข้อมูลจาก JSON
```

---

### ส่วนที่ 4: การตรวจสอบและการบำรุงรักษา

#### การตรวจสอบระบบ

**ตรวจสอบสถานะบริการ**:
```bash
# แบ็กเอนด์ (Node.js)
pm2 status
pm2 logs medkm-api

# แบ็กเอนด์ (Laravel)
sudo systemctl status php8.1-fpm
sudo systemctl status medkm-queue
tail -f /var/www/medkm/backend/laravel-real/storage/logs/laravel.log

# เว็บเซิร์ฟเวอร์
sudo systemctl status nginx
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log

# ฐานข้อมูล
sudo systemctl status mysql
```

#### กลยุทธ์การสำรองข้อมูล

**การสำรองฐานข้อมูล (Laravel)**:
```bash
# สร้างสคริปต์สำรอง
sudo nano /usr/local/bin/backup-medkm-db.sh
```
เพิ่ม:
```bash
#!/bin/bash
TIMESTAMP=$(date +"%Y%m%d_%H%M%S")
BACKUP_DIR="/var/backups/medkm"
mkdir -p $BACKUP_DIR

mysqldump -u medkm_user -p'your_password' medkm > $BACKUP_DIR/medkm_$TIMESTAMP.sql
gzip $BACKUP_DIR/medkm_$TIMESTAMP.sql

# เก็บเฉพาะ 7 วันล่าสุด
find $BACKUP_DIR -name "*.sql.gz" -mtime +7 -delete
```

ทำให้สามารถเรียกใช้งานได้:
```bash
sudo chmod +x /usr/local/bin/backup-medkm-db.sh
```

กำหนดการสำรองข้อมูลรายวัน:
```bash
sudo crontab -e
```
เพิ่ม:
```cron
0 2 * * * /usr/local/bin/backup-medkm-db.sh
```

#### การแก้ไขปัญหา

**แบ็กเอนด์ไม่ตอบสนอง**:
```bash
# Node.js
pm2 logs medkm-api --lines 100
pm2 restart medkm-api

# Laravel
tail -f /var/www/medkm/backend/laravel-real/storage/logs/laravel.log
sudo systemctl restart php8.1-fpm
```

**การเชื่อมต่อฐานข้อมูลล้มเหลว**:
```bash
# ทดสอบการเชื่อมต่อ MySQL
mysql -u medkm_user -p medkm

# ตรวจสอบสถานะ MySQL
sudo systemctl status mysql
sudo systemctl restart mysql

# เฉพาะ Laravel
cd /var/www/medkm/backend/laravel-real
php artisan config:clear
php artisan cache:clear
```

---

### ส่วนที่ 5: รายการตรวจสอบการผลิต

ก่อนเริ่มใช้งานจริง ตรวจสอบ:

- [ ] ติดตั้งใบรับรอง SSL และกำหนดค่าการต่ออายุอัตโนมัติ
- [ ] กำหนดค่าไฟร์วอลล์ (เปิดใช้งาน UFW เฉพาะพอร์ตที่จำเป็น)
- [ ] กำหนดเวลาสำรองฐานข้อมูล (cron job รายวัน)
- [ ] การหมุนเวียนบันทึกแอปพลิเคชัน (กำหนดค่า logrotate)
- [ ] ติดตั้งเครื่องมือตรวจสอบ (htop, netstat ฯลฯ)
- [ ] ตั้งค่าบริการ PM2/systemd ให้เริ่มอัตโนมัติเมื่อบูต
- [ ] กำหนดค่า Nginx ด้วย security headers ที่เหมาะสม
- [ ] ตั้งค่าสิทธิ์ไฟล์อย่างถูกต้อง (www-data สำหรับ Laravel)
- [ ] ตัวแปร environment ปลอดภัย (ไฟล์ .env ไม่สามารถเข้าถึงได้สาธารณะ)
- [ ] ข้อมูลประจำตัวฐานข้อมูลที่แข็งแกร่งและไม่ซ้ำกัน
- [ ] สร้างบัญชีผู้ใช้ทดสอบด้วยบทบาทที่ถูกต้อง
- [ ] API endpoints ตอบสนองอย่างถูกต้อง
- [ ] ส่วนหน้าโหลดและเชื่อมต่อกับแบ็กเอนด์
- [ ] ฟังก์ชันการเข้าสู่ระบบ/ออกจากระบบทำงาน
- [ ] การสร้างและการทำข้อสอบทำงาน
- [ ] ทดสอบการสร้าง PDF
- [ ] การอัปโหลดภาพทำงาน
- [ ] ทดสอบประสิทธิภาพภายใต้ภาระ
- [ ] การจัดการข้อผิดพลาดทำงานอย่างถูกต้อง
- [ ] ทดสอบการกู้คืนการสำรองข้อมูล

---

## บทบาทและความรับผิดชอบของผู้ใช้

### 👨‍⚕️ **1. นักเรียน (STUDENT)**

**วัตถุประสงค์:** มืออาชีพด้านการแพทย์และนักศึกษาแพทย์เรียนรู้ผ่านการสอบ

**สิทธิ์:** 
- ✅ ทำข้อสอบ (จำนวนครั้งไม่จำกัด)
- ✅ ดูสถิติและแนวโน้มความคืบหน้าส่วนบุคคล
- ✅ ดาวน์โหลดไฟล์ PDF ผลลัพธ์ข้อสอบ
- ✅ ดูคำตอบที่ถูกต้อง/ไม่ถูกต้องด้วยคำอธิบาย
- ❌ ไม่สามารถสร้างคำถาม
- ❌ ไม่สามารถจัดการผู้ใช้
- ❌ ไม่สามารถอนุมัติเนื้อหา

**เมตริกสำคัญ:**
- จำนวนข้อสอบที่ทำเสร็จ
- เปอร์เซ็นต์คะแนนเฉลี่ย
- คะแนนสูงสุดที่บรรลุ
- เปอร์เซ็นต์การปรับปรุง
- ประวัติข้อสอบพร้อมวันที่และคะแนน

**รายการเมนู:**
- หน้าแรก (แดชบอร์ด)
- ทำข้อสอบ
- นักเรียน (โปรไฟล์/สถิติ)
- หน้าสถิติส่วนบุคคล

---

### 💉 **2. จิตแพทย์ (CLINICIAN) - ผู้สร้างคำถาม**

**วัตถุประสงค์:** มืออาชีพด้านสุขภาพที่สร้างและตรวจสอบคำถามทางการแพทย์

**สิทธิ์:**
- ✅ สร้างคำถามทางการแพทย์ด้วยรูปภาพ
- ✅ อัปโหลดรูปภาพคำถาม
- ✅ ส่งคำถามเพื่อการตรวจสอบ
- ✅ จัดการคำถามของตนเอง
- ✅ ดูสถานะการอนุมัติ
- ✅ ทำข้อสอบ
- ✅ ดูแดชบอร์ดการวิเคราะห์
- ❌ ไม่สามารถอนุมัติคำถาม
- ❌ ไม่สามารถจัดการผู้ใช้

**ความรับผิดชอบสำคัญ:**
- เขียนคำถามที่มีคุณภาพสูงและมีความถูกต้องทางการแพทย์
- ให้คำอธิบายคำตอบที่ชัดเจนและอิงตามหลักฐาน
- แท็กคำถามด้วยความเชี่ยวชาญที่เหมาะสม
- ตรวจสอบให้แน่ใจว่ารูปภาพมีความเกี่ยวข้องและชัดเจน
- รักษาข้อกำหนดการจัดรูปแบบคำถาม
- รับข้อเสนอแนะจากผู้ดูแล

**รายการเมนู:**
- สร้างคำถาม
- จัดการ (การจัดการคำถาม)
- ผลลัพธ์ (ดูการส่ง)
- การวิเคราะห์ (แดชบอร์ด)

**รูปแบบคำถาม:**
```
ชื่อเรื่อง: ชื่อคำถาม
ความเชี่ยวชาญ: [โรคหัวใจ, ประสาทศาสตร์ ฯลฯ]
ระดับความยาก: [ง่าย, ปานกลาง, ยาก]
ประเภทคำถาม: [ตัวเลือกหลายตัว, จริง/เท็จ]
เนื้อหา: ข้อความคำถามพร้อมศัพท์ทางการแพทย์
ตัวเลือก: 4-5 คำตอบที่เป็นไปได้
คำตอบที่ถูกต้อง: ถูกทำเครื่องหมายไว้อย่างชัดเจน
คำอธิบาย: คำอธิบายโดยละเอียดของคำตอบที่ถูกต้อง
รูปภาพ: รูปภาพความละเอียดสูง มีป้ายชื่อถ้าจำเป็น
```

---

### ✅ **3. ผู้ดูแล (MODERATOR) - ผู้ตรวจสอบ QA/เนื้อหา**

**วัตถุประสงค์:** ตรวจสอบให้แน่ใจว่ามีคุณภาพและความถูกต้องของเนื้อหาทางการแพทย์

**สิทธิ์:**
- ✅ ตรวจสอบคำถามที่รอดำเนินการ
- ✅ อนุมัติ/ปฏิเสธคำถาม
- ✅ ส่งข้อเสนอแนะให้จิตแพทย์
- ✅ ดูการวิเคราะห์และสถิติ
- ✅ ทำข้อสอบ
- ✅ จัดการคำถาม
- ✅ ดูแดชบอร์ดจิตแพทย์
- ❌ ไม่สามารถจัดการผู้ใช้
- ❌ ไม่สามารถแก้ไขคำถามที่อนุมัติ

**ความรับผิดชอบสำคัญ:**
- ตรวจสอบคำถามที่ส่งมาเพื่อความถูกต้องทางการแพทย์
- ตรวจสอบเนื้อหาตามหลักฐาน
- ตรวจสอบการจัดรูปแบบและความชัดเจนของภาษา
- ตรวจสอบคุณภาพและความเกี่ยวข้องของรูปภาพ
- ให้ข้อเสนอแนะที่สร้างสรรค์
- รักษามาตรฐานหลักสูตร
- ปฏิเสธคำถามที่ไม่เป็นไปตามข้อกำหนดด้วยคำอธิบาย

**รายการตรวจสอบคุณภาพ:**
- [ ] ตรวจสอบความถูกต้องทางการแพทย์
- [ ] ภาษาชัดเจนและเป็นมืออาชีพ
- [ ] ไม่มีอคติหรือเนื้อหาที่ไม่พึงประสงค์
- [ ] รูปภาพเหมาะสมและมีป้ายชื่อ
- [ ] คำอธิบายคำตอบครอบคลุม
- [ ] อ้างอิงหากจำเป็น
- [ ] รูปแบบเป็นไปตามข้อกำหนด

**รายการเมนู:**
- ผู้ดูแล (คิวตรวจสอบ)
- การวิเคราะห์ (ข้อมูลประสิทธิภาพ)
- จัดการ (การจัดการเนื้อหา)

---

### 👨‍💼 **4. ผู้ดูแลระบบ (ADMIN) - ผู้บริหาร**

**วัตถุประสงค์:** การจัดการระบบโดยรวมและการบริหารผู้ใช้

**สิทธิ์:**
- ✅ สร้าง/แก้ไข/ลบบัญชีผู้ใช้
- ✅ กำหนดบทบาทผู้ใช้
- ✅ ดูการวิเคราะห์ที่ครอบคลุม
- ✅ ตรวจสอบกิจกรรมระบบ
- ✅ เข้าถึงทุกคุณลักษณะ
- ✅ กำหนดค่าการตั้งค่าระบบ
- ✅ ตรวจสอบบันทึกการตรวจสอบ
- ✅ จัดการฐานข้อมูล

**ความรับผิดชอบสำคัญ:**
- สร้างบัญชีผู้ใช้ใหม่
- กำหนดบทบาทที่เหมาะสม
- รีเซ็ตรหัสผ่านที่ลืม
- ตรวจสอบสุขภาพของระบบ
- สำรองข้อมูลเป็นประจำ
- อัปเดตซอฟต์แวร์ระบบ
- ตรวจสอบบันทึกความปลอดภัย
- สนับสนุนผู้ดูแลคนอื่น

**การจัดการผู้ใช้:**
- สร้างบัญชีสำหรับนักเรียน จิตแพทย์ ผู้ดูแล
- กำหนดบทบาทพร้อมสิทธิ์ที่เหมาะสม
- รีเซ็ตรหัสผ่านที่ลืม
- ปิดใช้งานบัญชีที่ไม่ใช้งาน
- รักษาความสมบูรณ์ของฐานข้อมูลผู้ใช้

**รายการเมนู:**
- ผู้ใช้ (การจัดการผู้ใช้)
- การวิเคราะห์ (แดชบอร์ดระบบ)
- สามารถเข้าถึงได้ทุกคุณลักษณะ

---

### 💻 **5. ผู้ดูแลระบบ IT (System & Infrastructure)**

**วัตถุประสงค์:** โครงสร้างพื้นฐานและการดำเนินการระบบทางเทคนิค

**ความรับผิดชอบ:**
- ติดตั้งและกำหนดค่าเซิร์ฟเวอร์
- จัดการใบรับรอง SSL/HTTPS
- กำหนดค่าเซิร์ฟเวอร์เว็บ (Nginx/Apache)
- การดำเนินการด้านฐานข้อมูล
- การสำรองข้อมูลและการกู้คืนจากการสูญหายข้อมูล
- การตรวจสอบประสิทธิภาพ
- การปรับปรุงความปลอดภัยและการอัปเดต
- การวิเคราะห์บันทึกและการตรวจสอบ
- การกำหนดค่าเครือข่าย
- การจัดการ Docker/Container

**งานหลัก:**
1. **การตั้งค่าเซิร์ฟเวอร์**
   - ติดตั้งแพคเกจ OS: Node.js, Nginx, PostgreSQL/MySQL
   - กำหนดค่ากฎไฟร์วอลล์
   - ตั้งค่าการเข้าถึง SSH
   - กำหนดค่าการตรวจสอบระบบ

2. **การทำให้แอปพลิเคชันใช้งานได้**
   - โคลนที่เก็บข้อมูล
   - ติดตั้งความต้องการ
   - กำหนดค่าตัวแปรสภาพแวดล้อม
   - เริ่มบริการ
   - ตรวจสอบบันทึก

3. **การจัดการฐานข้อมูล**
   - ตั้งค่าเซิร์ฟเวอร์ฐานข้อมูล
   - เรียกใช้การย้ายข้อมูล
   - สร้างสำรองข้อมูล
   - ตรวจสอบประสิทธิภาพ
   - จัดการการจำลองข้อมูล/การจัดกลุ่ม

4. **ความปลอดภัย**
   - สร้างใบรับรอง SSL
   - กำหนดค่าการตรวจสอบสิทธิ์
   - ใช้งานการจำกัดอัตrา
   - ตั้งค่า Web Application Firewall (WAF)
   - การตรวจสอบความปลอดภัยประจำ

5. **การตรวจสอบและการบำรุงรักษา**
   - การใช้ CPU/Memory/Disk
   - เวลาการตอบสนอง API
   - การวิเคราะห์บันทึกข้อผิดพลาด
   - การรีสตาร์ทอัตโนมัติเมื่อเกิดความล้มเหลว
   - หน้าต่างการบำรุงรักษาตามกำหนดเวลา

---

## คู่มือผู้ใช้ตามบทบาท

### 🎓 คู่มือนักเรียน

#### การเข้าสู่ระบบ
1. ไปที่หน้าเข้าสู่ระบบ
2. ป้อนอีเมลและรหัสผ่าน
3. คลิก "เข้าสู่ระบบ"
4. เปลี่ยนเส้นทางไปยังหน้าแรก

#### การทำข้อสอบ
1. คลิก "ทำข้อสอบ" ในแถบนำทาง
2. เลือกข้อสอบจากรายการ
3. คลิกปุ่ม "เริ่ม"
4. ตอบคำถามทั้งหมด
5. คลิก "ส่งข้อสอบ"
6. ดูผลลัพธ์และคะแนนทันที
7. คลิก "ดาวน์โหลด PDF" เพื่อพิมพ์ผลลัพธ์

#### การดูสถิติ
1. คลิก "นักเรียน" ในแถบนำทาง
2. ดูเมตริกประสิทธิภาพ:
   - จำนวนข้อสอบที่ทำเสร็จ
   - คะแนนเฉลี่ย
   - คะแนนสูงสุด
   - เปอร์เซ็นต์การปรับปรุง
3. ดูแนวโน้มคะแนนในแผนภูมิ
4. ดูการแบ่งต่อแต่ละข้อสอบ
5. ดาวน์โหลด PDF ข้อสอบเฉพาะ

#### การทำความเข้าใจคะแนน
- **คะแนนการผ่าน**: 70% ขึ้นไป = ผ่าน ✅
- **คะแนนไม่ผ่าน**: ต่ำกว่า 70% = ไม่ผ่าน ❌
- **การเข้ารหัสสีของคะแนน**:
  - สีเขียว: 70-100% (ดีเยี่ยม)
  - สีส้ม: 60-69% (ขอบเขต)
  - สีแดง: ต่ำกว่า 60% (ล้มเหลว)

---

### 🏥 คู่มือจิตแพทย์

#### สร้างคำถามใหม่
1. คลิก "สร้างคำถาม" ในแถบนำทาง
2. กรอกแบบฟอร์มด้วย:
   - **ชื่อเรื่อง**: คำถามที่ชัดเจนและเฉพาะเจาะจง
   - **ความเชี่ยวชาญ**: เลือกจากเมนูดรอปดาวน์
   - **ระดับความยาก**: ง่าย/ปานกลาง/ยาก
   - **ประเภทคำถาม**: ตัวเลือกหลายตัว
   - **เนื้อหาคำถาม**: ข้อความชัดเจน
   - **ตัวเลือก**: ให้ 4-5 คำตอบ
   - **คำตอบที่ถูกต้อง**: เลือกตัวเลือกที่ถูกต้อง
   - **คำอธิบาย**: คำอธิบายโดยละเอียด
   - **รูปภาพ**: อัปโหลดหากจำเป็น

3. คลิก "อัปโหลด"
4. คำถามจะเข้าคิวตรวจสอบ

#### อัปโหลดคำถามพร้อมรูปภาพ
1. คลิก "สร้างคำถาม"
2. กรอกช่องที่จำเป็นทั้งหมด
3. คลิก "อัปโหลดรูปภาพ"
4. เลือกรูปภาพความละเอียดสูง ที่มีป้ายชื่อ
5. รูปภาพปรากฏในการแสดงตัวอย่าง
6. ส่งแบบฟอร์ม

#### ติดตามสถานะคำถาม
1. คลิก "จัดการ" ในแถบนำทาง
2. ดูตารางคำถามของคุณ:
   - สถานะ: รอดำเนินการ/อนุมัติ/ปฏิเสธ
   - วันที่สร้าง
   - ข้อเสนอแนะจากผู้ดูแล
3. แก้ไขคำถามที่รอดำเนินการได้
4. ส่งใหม่หากปฏิเสธ
5. ดูคำถามที่อนุมัติทั้งหมด

#### ดูการวิเคราะห์
1. คลิก "การวิเคราะห์" ในแถบนำทาง
2. ดูประสิทธิภาพของนักเรียนในคำถามของคุณ
3. ดูอัตราการผ่านของนักเรียน
4. ติดตามความยากของคำถาม

---

### ✅ คู่มือผู้ดูแล

#### ตรวจสอบคำถาม
1. คลิก "ผู้ดูแล" ในแถบนำทาง
2. ดูคิวของคำถามที่รอดำเนินการ
3. คลิกคำถามเพื่อดูรายละเอียด:
   - ข้อความคำถาม
   - ตัวเลือกทั้งหมด
   - คำอธิบาย
   - รูปภาพถ้ามี
   - ชื่อจิตแพทย์

#### อนุมัติ/ปฏิเสธคำถาม
1. อ่านคำถามอย่างถี่ถ้วน
2. ตรวจสอบความถูกต้องทางการแพทย์
3. ตรวจสอบการจัดรูปแบบ
4. ตรวจสอบรูปภาพถ้ามี
5. คลิก "อนุมัติ" หรือ "ปฏิเสธ"
6. หากปฏิเสธ ให้เพิ่มข้อเสนอแนะ:
   - เหตุผลในการปฏิเสธ
   - การปรับปรุงที่มีความหมาย
7. ส่ง

#### ดูการวิเคราะห์
1. คลิก "การวิเคราะห์" ในแถบนำทาง
2. ดูแดชบอร์ดสามประการ:
   - **ภาพรวม**: สถิติแพลตฟอร์ม
   - **ประสิทธิภาพนักเรียน**: ติดตามแต่ละคน
   - **การวิเคราะห์ข้อสอบ**: การแบ่งรายละเอียด

#### มาตรฐานคุณภาพเพื่อตรวจสอบ
- ✅ ความถูกต้องทางการแพทย์
- ✅ เนื้อหาตามหลักฐาน
- ✅ ภาษาชัดเจน
- ✅ การจัดรูปแบบมืออาชีพ
- ✅ รูปภาพที่เหมาะสม
- ✅ ไม่มีอคติ/การแบ่งแยก
- ✅ คำอธิบายสมบูรณ์

---

### 👨‍💼 คู่มือผู้ดูแลระบบ

#### สร้างผู้ใช้ใหม่
1. คลิก "ผู้ใช้" ในแถบนำทาง
2. คลิก "เพิ่มผู้ใช้ใหม่"
3. กรอกแบบฟอร์ม:
   - **ชื่อ**: ชื่อเต็ม
   - **อีเมล**: ที่อยู่อีเมลที่ถูกต้อง
   - **รหัสผ่าน**: รหัสผ่านที่แข็งแกร่ง
   - **บทบาท**: เลือกจากเมนูดรอปดาวน์:
     - นักเรียน
     - จิตแพทย์
     - ผู้ดูแล
     - ผู้ดูแลระบบ
4. คลิก "สร้างผู้ใช้"
5. ผู้ใช้เพิ่มเข้าในระบบ
6. ผู้ใช้สามารถเข้าสู่ระบบด้วยอีเมล/รหัสผ่าน

#### แก้ไขผู้ใช้
1. คลิก "ผู้ใช้" ในแถบนำทาง
2. ค้นหาผู้ใช้ในตาราง
3. คลิกปุ่ม "แก้ไข"
4. อัปเดตข้อมูล:
   - ชื่อ
   - อีเมล
   - บทบาท
   - รหัสผ่าน (ตัวเลือก)
5. คลิก "บันทึก"

#### ลบผู้ใช้
1. คลิก "ผู้ใช้" ในแถบนำทาง
2. ค้นหาผู้ใช้ในตาราง
3. คลิกปุ่ม "ลบ"
4. ยืนยันการลบ
5. บัญชีผู้ใช้ถูกลบ

#### ดูรายชื่อผู้ใช้
1. คลิก "ผู้ใช้" ในแถบนำทาง
2. ดูตารางผู้ใช้ทั้งหมด:
   - ชื่อ
   - อีเมล
   - บทบาท (มีการเข้ารหัสสี)
   - วันที่สร้าง
   - การดำเนินการ (แก้ไข/ลบ)

#### ดูการวิเคราะห์
1. คลิก "การวิเคราะห์" ในแถบนำทาง
2. ดูแดชบอร์ดที่ครอบคลุม:
   - สถิติแพลตฟอร์ม
   - ความคืบหน้าของนักเรียน
   - ประสิทธิภาพข้อสอบ
   - การมีส่วนร่วมของจิตแพทย์

#### บัญชีทดสอบการเข้าถึง
ผู้ดูแลสามารถใช้บัญชีทดสอบเพื่อตรวจสอบระบบ:
- **นักเรียน**: student@test.com / รหัสผ่าน
- **จิตแพทย์**: clinician@test.com / รหัสผ่าน
- **ผู้ดูแล**: moderator@test.com / รหัสผ่าน
- **ผู้ดูแลระบบ**: admin@test.com / รหัสผ่าน

---

### 💻 คู่มือผู้ดูแลระบบ IT

#### การติดตั้งเซิร์ฟเวอร์

**Ubuntu/Debian:**
```bash
# อัปเดตระบบ
sudo apt-get update && sudo apt-get upgrade -y

# ติดตั้ง Node.js
curl -sL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# ติดตั้ง Nginx
sudo apt-get install -y nginx

# ติดตั้ง PostgreSQL (ตัวเลือก)
sudo apt-get install -y postgresql postgresql-contrib

# โคลนแอปพลิเคชัน
git clone <repo-url> /var/www/med-km
cd /var/www/med-km

# ติดตั้งความต้องการ
cd backend/mock-api && npm install
cd ../../frontend && npm install
```

#### กำหนดค่า Nginx
สร้าง `/etc/nginx/sites-available/med-km`:
```nginx
upstream med_km_backend {
    server 127.0.0.1:3001;
}

server {
    listen 80;
    server_name your-domain.com;
    
    # เปลี่ยนเส้นทาง HTTP เป็น HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    # ส่วนหน้า
    location / {
        root /var/www/med-km/frontend/dist;
        try_files $uri $uri/ /index.html;
    }

    # API แบ็กเอนด
    location /api {
        proxy_pass http://med_km_backend;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header Authorization $http_authorization;
        
        # ส่วนหัว CORS
        add_header Access-Control-Allow-Origin * always;
        add_header Access-Control-Allow-Methods "GET, POST, PUT, DELETE, OPTIONS" always;
        add_header Access-Control-Allow-Headers "Content-Type, Authorization" always;
    }
}
```

เปิดใช้งานไซต์:
```bash
sudo ln -s /etc/nginx/sites-available/med-km /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl restart nginx
```

#### เริ่มแอปพลิเคชัน

**แบ็กเอนด (PM2):**
```bash
sudo npm install -g pm2
cd /var/www/med-km/backend/mock-api
pm2 start index.js --name "med-km-api" --env production
pm2 save
pm2 startup
```

**ส่วนหน้า:**
```bash
cd /var/www/med-km/frontend
npm run build
# ให้บริการโดย Nginx จากโฟลเดอร์ dist/
```

#### ตรวจสอบระบบ
```bash
# ตรวจสอบบริการ
pm2 status
pm2 logs med-km-api

# ตรวจสอบทรัพยากร
htop

# ตรวจสอบ Nginx
sudo tail -f /var/log/nginx/error.log
sudo tail -f /var/log/nginx/access.log

# ฐานข้อมูล
sudo -u postgres psql
```

#### การบำรุงรักษาประจำ
```bash
# สำรองข้อมูลรายสัปดาห์
0 2 * * 0 /usr/local/bin/backup.sh

# การหมุนบันทึก
sudo logrotate -f /etc/logrotate.d/med-km

# อัปเดตระบบ
sudo apt-get update && sudo apt-get upgrade -y

# ต่ออายุ SSL (อัตโนมัติผ่าน certbot)
sudo certbot renew --quiet
```

---

## เอกสาร API

### 🔑 การตรวจสอบสิทธิ์

**จุดสิ้นสุดเข้าสู่ระบบ:**
```
POST /api/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "password123"
}

ตอบสนอง (200 OK):
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": "user-123",
    "name": "John Doe",
    "email": "user@example.com",
    "role": "student"
  }
}
```

**คำขอ API ทั้งหมดต้องการโทเค็น:**
```
Authorization: Bearer {token}
```

### 📚 API ข้อสอบ

**รับข้อสอบทั้งหมด:**
```
GET /api/exams
Authorization: Bearer {token}

ตอบสนอง:
[
  {
    "id": "exam-1",
    "title": "พื้นฐานการแพทย์",
    "description": "บทนำสู่",
    "totalQuestions": 10,
    "createdBy": "clinician-123",
    "createdAt": "2026-02-18"
  }
]
```

### 📊 Student Stats API

**รับสถิติส่วนบุคคล:**
```
GET /api/my-stats
Authorization: Bearer {token}

ตอบสนอง:
{
  "attempts": [
    {
      "id": "result-1",
      "examTitle": "พื้นฐานการแพทย์",
      "score": 85,
      "passed": true,
      "date": "2026-02-18",
      "correct": 8,
      "total": 10,
      "passingScore": 70
    }
  ],
  "avgScore": 82,
  "bestScore": 95,
  "improvement": 12
}
```

---

## การแก้ปัญหา

### ปัญหาทั่วไปและแนวทางแก้ไข

#### 1. ไม่สามารถเริ่มแบ็กเอนด
**ปัญหา:** `npm start` ล้มเหลวด้วยข้อผิดพลาด

**แนวทางแก้ไข:**
```bash
# ล้างแคช
rm -rf node_modules package-lock.json
npm install

# ตรวจสอบเวอร์ชัน Node
node --version  # ควรเป็น v14+

# ตรวจสอบว่าพอร์ตว่างหรือไม่
lsof -i :3001

# ฆ่าโปรเซสบนพอร์ต
kill -9 <PID>
```

#### 2. ส่วนหน้าจะไม่โหลด
**ปัญหา:** หน้าว่างหรือข้อผิดพลาด 502

**แนวทางแก้ไข:**
```bash
# สร้างใหม่ส่วนหน้า
npm run build

# ล้างแคชเบราว์เซอร์
# Ctrl+Shift+Delete

# ตรวจสอบการเชื่อมต่อ API
# fetch('http://localhost:3001/api')
```

#### 3. การเข้าสู่ระบบล้มเหลว
**ปัญหา:** ข้อผิดพลาด "ข้อมูลประจำตัวไม่ถูกต้อง"

**แนวทางแก้ไข:**
- ตรวจสอบว่าผู้ใช้มีอยู่
- ตรวจสอบอีเมลตรงกันทุกประการ (มีความแ民ไหม)
- รหัสผ่านถูกต้อง
- เซิร์ฟเวอร์แบ็กเอนดกำลังทำงาน

#### 4. ฐานข้อมูลไม่เชื่อมต่อ
**ปัญหา:** ไม่สามารถเชื่อมต่อ PostgreSQL/MySQL

**แนวทางแก้ไข:**
```bash
# ทดสอบการเชื่อมต่อ
psql -U username -d database_name
mysql -u username -p database_name

# ตรวจสอบข้อมูลประจำตัวใน .env
cat .env

# เริ่มบริการฐานข้อมูล
sudo service postgresql start
sudo service mysql start
```

---

## แนวทางความปลอดภัย

### 🔒 แนวปฏิบัติที่ดีที่สุด

#### ความปลอดภัยของการตรวจสอบสิทธิ์
1. **รหัสผ่านที่แข็งแกร่ง**
   - อักขระขั้นต่ำ 8 ตัว
   - ส่วนผสมของตัวพิมพ์ใหญ่ ตัวพิมพ์เล็ก ตัวเลข สัญลักษณ์
   - เปลี่ยนเป็นประจำ (ทุก 90 วัน)

2. **การจัดการโทเค็น**
   - โทเค็นหมดอายุหลังจากเซสชัน
   - ไม่ใช้ร่วมโทเค็น
   - เก็บไว้ใน localStorage เท่านั้น
   - ล้างเมื่อออกจากระบบ

#### การปกป้องข้อมูล
1. **การเข้ารหัส**
   - รหัสผ่านแฮชด้วย bcrypt
   - HTTPS/TLS สำหรับการส่งข้อมูลทั้งหมด
   - การเข้ารหัสฐานข้อมูลที่เหลือ

2. **การสำรองข้อมูลและการกู้คืน**
   - สำรองข้อมูลอัตโนมัติทุกวัน
   - เก็บสำรองข้อมูลในตำแหน่งที่ปลอดภัย
   - ทดสอบกระบวนการกู้คืนทุกเดือน
   - การเก็บรักษาสำรองข้อมูล: ขั้นต่ำ 30 วัน

#### การตรวจสอบและการตรวจสอบ
1. **การบันทึก**
   - บันทึกการดำเนินการของผู้ดูแลทั้งหมด
   - บันทึกความพยายามในการเข้าสู่ระบบที่ล้มเหลว
   - บันทึกการแก้ไขข้อมูล
   - รักษาบันทึกเป็นเวลา 90 วันขั้นต่ำ

2. **การตรวจสอบ**
   - ตรวจสอบ CPU/Memory/Disk
   - ตรวจสอบกิจกรรมที่ผิดปกติ
   - การตรวจสอบความปลอดภัยประจำ
   - การทดสอบการแทรกซึม (รายไตรมาส)

### 🚨 รายการตรวจสอบความปลอดภัย

- [ ] HTTPS/SSL เปิดใช้งานในทุกหน้า
- [ ] รหัสผ่านฐานข้อมูลแข็งแกร่งและไม่ซ้ำกัน
- [ ] บัญชีผู้ดูแลมี 2FA เปิดใช้งาน
- [ ] กำหนดค่าการสำรองข้อมูลประจำ
- [ ] ข้อความข้อผิดพลาดไม่เปิดเผยข้อมูลที่ละเอียดอ่อน
- [ ] จุดสิ้นสุด API ที่ตรวจสอบสิทธิ์
- [ ] CORS ได้รับการกำหนดค่าอย่างถูกต้อง
- [ ] มี rate limiting เปิดใช้งาน
- [ ] การป้องกัน SQL injection ทำงาน
- [ ] มี XSS protection เปิดใช้งาน
- [ ] โทเค็น CSRF บนแบบฟอร์ม
- [ ] ตรวจสอบช่องโหว่ของการพึ่งพา (`npm audit`)
- [ ] ตรวจสอบบันทึกการเข้าถึงเป็นรายสัปดาห์
- [ ] ทำการทดสอบการแทรกซึม
- [ ] ทดสอบแผนการกู้คืนจากการสูญหายข้อมูล

---

## การบำรุงรักษาและการสนับสนุน

### 📅 ตารางการบำรุงรักษา

#### รายวัน
- [ ] ตรวจสอบบันทึกระบบ
- [ ] ตรวจสอบบันทึกข้อผิดพลาด
- [ ] ตรวจสอบว่าบริการกำลังทำงาน
- [ ] ตรวจสอบการใช้พื้นที่ดิสก์

#### รายสัปดาห์
- [ ] ตรวจสอบบันทึกการเข้าถึง
- [ ] อัปเดตการแก้ไขความปลอดภัย
- [ ] ตรวจสอบการสำเร็จการสำรองข้อมูล
- [ ] ตรวจสอบประสิทธิภาพ

#### รายเดือน
- [ ] สำรองข้อมูลระบบทั้งหมด
- [ ] เพิ่มประสิทธิภาพฐานข้อมูล
- [ ] ตรวจสอบการเข้าถึงผู้ใช้
- [ ] การวางแผนความจุ
- [ ] การตรวจสอบความปลอดภัย

#### รายไตรมาส
- [ ] การทดสอบการแทรกซึม
- [ ] การฝึกความพร้อมสำหรับการกู้คืนจากการสูญหายข้อมูล
- [ ] อัปเดตการพึ่งพา
- [ ] ตรวจสอบความเป็นไปตามข้อกำหนด

### 📞 ทรัพยากรสนับสนุน

**เอกสารประกอบ:**
- คู่มือโครงการ (ไฟล์นี้)
- เอกสาร API
- คู่มือผู้ใช้
- คู่มือการแก้ปัญหา

**ติดต่อสนับสนุน:**
- อีเมล: it-admin@chomthong-hospital.com
- โทรศัพท์: +66-XX-XXX-XXXX ส่วนขยาย XXXX
- Help Desk: support@hospital.com
- ชั่วโมง: จันทร์-ศุกร์ 08:00 น.-17:00 น.

**การขยายขนาด:**
1. ระดับที่ 1: Help Desk / สนับสนุน IT
2. ระดับที่ 2: ผู้ดูแลระบบ IT อาวุโส
3. ระดับที่ 3: CIO / ผู้อำนวยการ IT

### 📝 การรายงานปัญหา

**เมื่อรายงานปัญหา ให้รวม:**
1. บทบาทและชื่อบัญชีผู้ใช้
2. วันที่และเวลาของปัญหา
3. ข้อความข้อผิดพลาดที่แน่นอน
4. ขั้นตอนในการทำซ้ำ
5. ภาพหน้าจอ/บันทึก หากมี
6. ข้อมูลเบราว์เซอร์/OS
7. สภาพแวดล้อมเครือข่าย (สำนักงาน/ระยะไกล)

**ส่งตามข้อมูล:**
- ระบบตั๋วเสบียง Help desk
- อีเมลพร้อมแท็ก [med-km]
- โดยตรงที่ออฟฟิศ IT

---

## ประวัติเวอร์ชัน

| เวอร์ชัน | วันที่ | การเปลี่ยนแปลง |
|---------|--------|-----------|
| 1.0 | 2026-02-18 | เปิดตัวครั้งแรกพร้อมคุณลักษณะหลัก |
| 1.1 | TBD | การตรวจสอบสิทธิ์ด้วยรหัส Thai ID |
| 1.2 | TBD | การวิเคราะห์ขั้นสูง |
| 2.0 | TBD | เปิดตัวแอปมือถือ |

---

## ภาคผนวก

### ภาคผนวก ก: แบบฟอร์มสร้างบัญชีผู้ใช้

```
แบบฟอร์มการสร้างผู้ใช้:
ชื่อ: _________________________
อีเมล: _________________________
บทบาทที่กำหนด: [ ] นักเรียน [ ] จิตแพทย์ [ ] ผู้ดูแล [ ] ผู้ดูแลระบบ
รหัสผ่านเริ่มต้น: _________________________
แผนก/ความเชี่ยวชาญ: _________________________
วันที่สร้าง: _________________________
สร้างโดย: _________________________
```

### ภาคผนวก ข: รายการตรวจสอบการติดตั้ง

- [ ] ตรวจสอบข้อกำหนดเบื้องต้น (Node.js v14+, Git)
- [ ] โคลนที่เก็บข้อมูล
- [ ] ติดตั้งความต้องการของแบ็กเอนด
- [ ] ติดตั้งความต้องการของส่วนหน้า
- [ ] ตรวจสอบไวยากรณ์แบ็กเอนด
- [ ] สร้างส่วนหน้าเสร็จสมบูรณ์
- [ ] เซิร์ฟเวอร์แบ็กเอนดเริ่มต้น
- [ ] เซิร์ฟเวอร์ส่วนหน้าเริ่มต้น
- [ ] API ตอบสนองต่อคำขอ
- [ ] สามารถเข้าสู่ระบบด้วยบัญชีทดสอบ
- [ ] สามารถดูหน้าเว็บสำหรับแต่ละบทบาท
- [ ] ตรวจสอบการเชื่อมต่อฐานข้อมูล
- [ ] บันทึกกำลังทำงาน
- [ ] กำหนดค่าการสำรองข้อมูล

### ภาคผนวก ค: อภิธานศัพท์

- **JWT**: JSON Web Token สำหรับการตรวจสอบสิทธิ์
- **CORS**: Cross-Origin Resource Sharing
- **API**: Application Programming Interface
- **CLI**: Command Line Interface
- **SSH**: Secure Shell สำหรับการเข้าถึงระยะไกล
- **SSL/TLS**: โปรโตคอลการติดต่อสื่อสารที่ปลอดภัย
- **PM2**: Process manager สำหรับ Node.js
- **PDFKit**: ไลบรารีสำหรับการสร้าง PDF

---

## ข้อมูลเอกสาร

**ชื่อเอกสาร:** แพลตฟอร์มการเรียนรู้ทางการแพทย์โรงพยาบาลจังหวัรชม่อง - คู่มือที่ครอบคลุม  
**เวอร์ชัน:** 1.0  
**อัปเดตล่าสุด:** 18 กุมภาพันธ์ 2569  
**ผู้เขียน:** แผนกเทคโนโลยีสารสนเทศ - โรงพยาบาลจังหวัรชม่อง  
**การจัดหมวดหมู่:** สำหรับการใช้ภายในเท่านั้น  
**การจัดจำหน่าย:** สำหรับผู้มีส่วนได้ส่วนเสียทั้งหมด (นักเรียน จิตแพทย์ ผู้ดูแล ผู้ดูแลระบบ IT สตาฟ)

---

**สำหรับคำถามหรือข้อเสนอแนะเกี่ยวกับคู่มือนี้ โปรดติดต่อ:**
- แผนกเทคโนโลยีสารสนเทศ: it-admin@chomthong-hospital.com
- เจ้าของเอกสาร: [ชื่อผู้อำนวยการ IT]

**วันที่ตรวจสอบครั้งต่อไป:** 18 สิงหาคม 2569

---

*เอกสารนี้เป็นเอกสารที่มีพลวัตและจะได้รับการอัปเดตเมื่อระบบพัฒนา ขอแนะนำให้ผู้ใช้ทั้งหมดให้ข้อเสนอแนะสำหรับการปรับปรุง*
